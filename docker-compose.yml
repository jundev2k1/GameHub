services:
  game-x.db:
    image: postgres:16.4

  game-x.api:
    image: game-x.api
    build:
      context: .
      dockerfile: game-x.api/Dockerfile

  game-x.seq:
    image: datalust/seq:latest

networks:
  dls-gamex:
    external: true

volumes:
  seq-data:
  db-data:
